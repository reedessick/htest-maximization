#!/usr/bin/env python

"""a script to fit the selected data
"""
__author__ = "Reed Essick (reed.essick@gmail.com)"

#-------------------------------------------------

import os

import numpy as np

from argparse import ArgumentParser

#-------------------------------------------------

parser = ArgumentParser(description=__doc__)

parser.add_argument('-v', '--verbose', default=False, action='store_true')

parser.add_argument('-o', '--output-dir', default='.', type=str)
parser.add_argument('-t', '--tag', default='', type=str)

args = parser.parse_args()

if not os.path.exists(args.output_dir):
    os.makedirs(args.output_dir)

if args.tag:
    args.tag = "_"+args.tag

#-------------------------------------------------

raise NotImplementedError('''\
compute a MLE fit for the signal parameters?
  --> estimate distribution from many different realizations and therefore the bias
estimate fisher matrix for signal parameters?

### NOTE: I need to somehow write the selected subsets of data to disk within downselect-data (or at least which trials to pick up from the make-data output)
  --> this will let me identify subsets of data in a simple way within this script

run a full MCMC and show that I get diagonal p-p plots?
  --> too expensive?
  --> would make a damned convincing argument

NOTE: we may want to support more general light-curve shapes within our mock-data generation. We can either specify these as long-winded sets of harmonics or we can just write code that will take in an arbirary (periodic) function of phi and use that.
''')
